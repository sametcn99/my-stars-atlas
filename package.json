{
	"name": "my-stars",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "bun run src/build-site.ts",
		"update": "bun run src/index.ts",
		"preview": "bunx serve dist --listen 4173",
		"typecheck": "bunx tsc --noEmit",
		"check": "bun run typecheck && bunx --bun @biomejs/biome check --write ."
	},
	"dependencies": {
		"handlebars": "^4.7.9"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.2",
		"@types/bun": "^1.3.14",
		"@types/handlebars": "^4.1.0",
		"bun-types": "^1.3.14",
		"typescript": "^6.0.3"
	}
}
